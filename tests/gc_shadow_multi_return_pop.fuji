// Regression: multi-return functions must emit fuji_pop_frame on every ret path.
func f() {
    if (false) {
        return 1;
    }
    return 0;
}

assert(f() == 0);
print("PASS gc_shadow_multi_return_pop");
