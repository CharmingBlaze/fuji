Fuji Language Specification v2.0
"Fortune in code - simple draws, powerful results"

Why Fuji?

🎴 Fuji (くじ) = Japanese for "lottery ticket" or "draw" - getting lucky, instant gratification
Short (4 letters), unique, memorable, .fuji extension
Easy to say in any language
Suggests: fortune, quick results, magical simplicity
"Draw your code into existence"
Available everywhere, completely unique
Cultural cachet without being pretentious

Tagline: "Draw it. Run it. Ship it."

Core Philosophy (Enhanced)
Fuji is a powerful scripting language that feels like JavaScript but compiles to C speed:

Easy to learn - JavaScript-familiar syntax, minimal keywords
Easy to extend - Simple library system with #include
Powerful interop - Auto-wrap any C/C++ library with KujiWrap
Production ready - Compile to native binaries
Zero ceremony - No headers to write, no build config

Fuji = Lua's simplicity + JavaScript's syntax + C's speed + effortless C/C++ interop

Module System: Simple Includes
Basic Include Syntax
javascript#include "math_utils.fuji"
#include "player.fuji"
#include "libs/json.fuji"

let x = math_utils.sqrt(16);
let p = player.create("Jesse");
How It Works
my_game.fuji
├─ #include "player.fuji"
│   └─ exports: player.create(), player.update()
├─ #include "enemy.fuji"  
│   └─ exports: enemy.spawn(), enemy.ai()
└─ main()
Rules:

Files are included once (no duplicate loading)
Circular includes are detected and throw error
Path resolution: relative to current file, or from FUJI_PATH
All top-level functions/variables in included file become accessible via module namespace


Library System
Creating a Fuji Library
File: math_utils.fuji
javascript// Everything at top level is exported automatically
func square(x) {
    return x * x;
}

func cube(x) {
    return x * x * x;
}

let PI = 3.14159;

// Private helper (prefix with _ to hide from exports)
func _internal_calc(x) {
    return x * 2;
}
Using the library:
javascript#include "math_utils.fuji"

print(math_utils.square(5));     // 25
print(math_utils.PI);             // 3.14159
print(math_utils._internal_calc); // Error: private function
Namespace Control
javascript// Import with custom namespace
#include "math_utils.fuji" as math

print(math.square(5));

// Import specific functions into current scope
#include "math_utils.fuji" {square, cube}

print(square(5));  // No prefix needed

C/C++ Interop: KujiWrap Tool
The Magic: Auto-Generate Bindings
KujiWrap reads C/C++ headers and generates Fuji bindings automatically.
bash# Generate Fuji wrapper from C library
kujiwrap raylib.h -o raylib.fuji

# Now use in your code
#include "raylib.fuji"

raylib.InitWindow(800, 600, "My Game");

KujiWrap Architecture
Step 1: Parse C/C++ Headers
KujiWrap uses libclang to parse headers:
bashkujiwrap SDL2/SDL.h -o sdl2.fuji
What it extracts:

Function signatures
Struct definitions
Enum values
Constants
Documentation comments

Step 2: Generate Fuji Module
Input: raylib.h
c// Initialize window
void InitWindow(int width, int height, const char *title);

// Draw circle
void DrawCircle(int centerX, int centerY, float radius, Color color);

typedef struct Color {
    unsigned char r;
    unsigned char g;
    unsigned char b;
    unsigned char a;
} Color;
Output: raylib.fuji (auto-generated)
javascript// Auto-generated by KujiWrap from raylib.h
// Do not edit manually

#native "raylib"

/// Initialize window
/// @param width - Window width in pixels
/// @param height - Window height in pixels  
/// @param title - Window title
func InitWindow(width, height, title) {
    _ffi_call("InitWindow", width, height, title);
}

/// Draw circle on screen
/// @param centerX - Center X coordinate
/// @param centerY - Center Y coordinate
/// @param radius - Circle radius
/// @param color - Circle color (Color object)
func DrawCircle(centerX, centerY, radius, color) {
    _ffi_call("DrawCircle", centerX, centerY, radius, color);
}

// Color constructor with validation
func Color(r, g, b, a) {
    return {
        r: r,
        g: g,
        b: b,
        a: a or 255
    };
}

// Color constants
let RED = Color(255, 0, 0, 255);
let GREEN = Color(0, 255, 0, 255);
let BLUE = Color(0, 0, 255, 255);

KujiWrap Features
1. Smart Type Mapping
C TypeKuji TypeHandlingint, longnumberDirect conversionfloat, doublenumberDirect conversionchar*stringAuto UTF-8 conversionvoid*objectOpaque pointer wrapperstructobjectField-by-field mappingenumConstantsExported as globalsFunction pointerfunctionCallable from Fuji
2. Memory Management
javascript// KujiWrap handles cleanup automatically
#include "sqlite.fuji"

let db = sqlite.open("data.db");  // Opens database
// ... use database
// db auto-closed when out of scope (via GC finalizer)
Behind the scenes:
c// Generated C binding
static Value FUJI_sqlite_open(Value filename) {
    sqlite3* db;
    sqlite3_open(AS_STRING(filename), &db);
    
    // Wrap in GC-tracked object with finalizer
    return wrap_pointer(db, sqlite_finalizer);
}

static void sqlite_finalizer(void* ptr) {
    sqlite3_close((sqlite3*)ptr);
}
3. Error Handling
javascript#include "curl.fuji"

func download(url) {
    let result = curl.get(url);
    
    if (result.error) {
        warn("Download failed:", result.error);
        return null;
    }
    
    return result.body;
}
Generated wrapper includes error checking:
cstatic Value FUJI_curl_get(Value url) {
    CURLcode res = curl_easy_perform(curl, AS_STRING(url));
    
    if (res != CURLE_OK) {
        // Return object with error field
        return make_object({
            {"error", make_string(curl_easy_strerror(res))}
        });
    }
    
    return make_object({
        {"body", make_string(response_buffer)},
        {"status", make_number(http_code)}
    });
}
4. Documentation Preservation
KujiWrap extracts Doxygen/JSDoc-style comments:
c/**
 * @brief Loads an image from file
 * @param filename Path to image file (PNG, JPG, etc)
 * @return Image object or NULL on failure
 * @example 
 *   Image img = LoadImage("player.png");
 */
Image LoadImage(const char *filename);
Generated Fuji includes docs:
javascript/// Loads an image from file
/// @param filename - Path to image file (PNG, JPG, etc)
/// @returns Image object or null on failure
/// @example
///   let img = raylib.LoadImage("player.png");
func LoadImage(filename) {
    return _ffi_call("LoadImage", filename);
}

KujiWrap Configuration
Wrapper Config File: kujiwrap.toml
toml[library]
name = "raylib"
headers = ["raylib.h", "raymath.h"]
libs = ["raylib"]
include_paths = ["/usr/local/include"]

[options]
# Functions to skip (internal/deprecated)
exclude = ["rlgl*", "Internal*"]

# Rename functions for clarity
rename = [
    { from = "InitWindow", to = "init_window" },
    { from = "DrawCircle", to = "draw_circle" }
]

# Custom type mappings
types = [
    { c_type = "Vector2", FUJI_type = "object", fields = ["x", "y"] }
]

[memory]
# Auto-free these types when GC'd
auto_free = ["Image", "Texture", "Sound"]

# Reference-counted types
ref_counted = ["Mesh", "Model"]
Run KujiWrap
bash# Basic usage
kujiwrap raylib.h -o raylib.fuji

# With config file
kujiwrap --config kujiwrap.toml

# Multiple headers
kujiwrap SDL2/*.h -o sdl2.fuji

# With documentation
kujiwrap --docs raylib.h -o raylib.fuji

# Generate both .fuji module and .md docs
kujiwrap --docs --markdown raylib.h -o raylib.fuji

KujiWrap Implementation
Technology Stack
KujiWrap Tool
├─ libclang (parse C/C++ headers)
├─ Template engine (generate Fuji code)
├─ Type inference (smart conversions)
└─ Documentation extractor
Core Algorithm
python# Simplified KujiWrap logic (Python pseudocode)

def generate_wrapper(header_file):
    # 1. Parse with libclang
    index = clang.Index.create()
    tu = index.parse(header_file)
    
    # 2. Extract declarations
    functions = extract_functions(tu.cursor)
    structs = extract_structs(tu.cursor)
    enums = extract_enums(tu.cursor)
    
    # 3. Generate Fuji code
    output = []
    output.append('#native "mylibrary"\n')
    
    for func in functions:
        output.append(generate_function_wrapper(func))
    
    for struct in structs:
        output.append(generate_struct_constructor(struct))
    
    for enum in enums:
        output.append(generate_enum_constants(enum))
    
    return '\n'.join(output)

def generate_function_wrapper(func):
    params = ', '.join(p.name for p in func.params)
    doc = extract_documentation(func)
    
    return f'''
{doc}
func {func.name}({params}) {{
    return _ffi_call("{func.name}", {params});
}}
'''

Example: Wrapping SQLite
1. Generate Wrapper
bashkujiwrap sqlite3.h -o sqlite.fuji
2. Use in Fuji
javascript#include "sqlite.fuji"

func main() {
    // Open database
    let db = sqlite.open("users.db");
    
    // Create table
    sqlite.exec(db, "CREATE TABLE users (id INT, name TEXT)");
    
    // Insert data
    let stmt = sqlite.prepare(db, "INSERT INTO users VALUES (?, ?)");
    sqlite.bind_int(stmt, 1, 42);
    sqlite.bind_text(stmt, 2, "Jesse");
    sqlite.step(stmt);
    
    // Query data
    let stmt2 = sqlite.prepare(db, "SELECT * FROM users");
    while (sqlite.step(stmt2) == sqlite.ROW) {
        let id = sqlite.column_int(stmt2, 0);
        let name = sqlite.column_text(stmt2, 1);
        print("User:", id, name);
    }
    
    // Auto-closed by GC
}

Standard Library (Built-in)
Fuji ships with batteries included:
javascript// No #include needed for stdlib

// I/O
print("Hello");
warn("Error message");
let input = read_line("Enter name: ");

// Math
let x = abs(-5);
let y = sqrt(16);
let z = random();

// Strings
let s = "hello";
print(s.upper());      // "HELLO"
print(s.length());     // 5
print(s.split("l"));   // ["he", "", "o"]

// Arrays
let arr = [1, 2, 3];
arr.push(4);
arr.pop();
print(arr.length());

// Objects
let obj = {x: 10, y: 20};
print(obj.x);
obj.z = 30;

// Files
let content = read_file("data.txt");
write_file("output.txt", content);

// JSON
let data = json_parse('{"x": 10}');
let text = json_stringify(data);

// Time
let now = time();
sleep(1000);  // 1 second

Official Libraries (Downloadable)
Package Manager: kuji pkg
bash# Install library from registry
kuji pkg install raylib
kuji pkg install sqlite
kuji pkg install curl

# Libraries go to ~/.fuji/packages/
Registry structure:
packages/
├─ raylib/
│   ├─ raylib.fuji      (wrapper)
│   ├─ libraylib.so     (native lib)
│   └─ examples/
├─ sqlite/
│   ├─ sqlite.fuji
│   └─ libsqlite3.so
└─ curl/
    ├─ curl.fuji
    └─ libcurl.so
Use installed package:
javascript#include <raylib>  // <> means system package

raylib.InitWindow(800, 600, "Game");

Creating Your Own C Library Wrapper
Example: Wrapping a Custom C Library
Your C library: mylib.h
c// mylib.h
typedef struct {
    int x;
    int y;
} Point;

Point create_point(int x, int y);
void print_point(Point p);
int distance(Point a, Point b);
Generate wrapper:
bashkujiwrap mylib.h -o mylib.fuji -l mylib
Generated mylib.fuji:
javascript#native "mylib"

/// Create a new Point
func create_point(x, y) {
    return _ffi_call("create_point", x, y);
}

/// Print point coordinates
func print_point(p) {
    _ffi_call("print_point", p);
}

/// Calculate distance between two points
func distance(a, b) {
    return _ffi_call("distance", a, b);
}

// Helper constructor
func Point(x, y) {
    return create_point(x, y);
}
Use it:
javascript#include "mylib.fuji"

let p1 = mylib.Point(10, 20);
let p2 = mylib.Point(30, 40);

mylib.print_point(p1);
let d = mylib.distance(p1, p2);
print("Distance:", d);

Complete Example: Game with Raylib
javascript#include <raylib>

let player = {
    x: 400,
    y: 300,
    speed: 5
};

func update() {
    if (raylib.IsKeyDown(raylib.KEY_RIGHT)) {
        player.x += player.speed;
    }
    if (raylib.IsKeyDown(raylib.KEY_LEFT)) {
        player.x -= player.speed;
    }
    if (raylib.IsKeyDown(raylib.KEY_DOWN)) {
        player.y += player.speed;
    }
    if (raylib.IsKeyDown(raylib.KEY_UP)) {
        player.y -= player.speed;
    }
}

func draw() {
    raylib.BeginDrawing();
    raylib.ClearBackground(raylib.BLACK);
    
    raylib.DrawCircle(player.x, player.y, 20, raylib.RED);
    raylib.DrawText("Move with arrows", 10, 10, 20, raylib.WHITE);
    
    raylib.EndDrawing();
}

func main() {
    raylib.InitWindow(800, 600, "Fuji Game");
    raylib.SetTargetFPS(60);
    
    while (!raylib.WindowShouldClose()) {
        update();
        draw();
    }
    
    raylib.CloseWindow();
}
Compile and run:
bashfuji run game.fuji
# Or build native binary
fuji build game.fuji -o game
./game

Advanced: Custom FFI
For complete control, manually define FFI:
javascript#native "mylib"

// Manual FFI declaration
#ffi func add(int, int) -> int;
#ffi func get_name() -> string;
#ffi func process_data(ptr) -> void;

// Now use them
let result = add(10, 20);
let name = get_name();

KujiWrap CLI Reference
bash# Generate wrapper
kujiwrap <header.h> [options]

Options:
  -o, --output <file>        Output .fuji file
  -l, --link <lib>           Library to link against
  -I, --include <path>       Add include path
  -c, --config <file>        Use config file
  --docs                     Extract documentation
  --markdown                 Generate markdown docs
  --exclude <pattern>        Exclude functions matching pattern
  --rename <from:to>         Rename function
  --prefix <prefix>          Add prefix to all functions
  --no-constants             Don't generate enum constants
  --no-structs               Don't generate struct constructors

Examples:
  kujiwrap SDL2/SDL.h -o sdl2.fuji -l SDL2
  kujiwrap raylib.h -o raylib.fuji --docs
  kujiwrap mylib.h -c kujiwrap.toml
  kujiwrap --exclude "Internal*" api.h -o api.fuji

Compiler Commands
bash# Run script (bytecode VM)
fuji run game.fuji

# Build native binary (fast)
fuji build game.fuji -o game

# Build with optimizations (fastest)
fuji build game.fuji -o game --release

# Watch mode (auto-reload on file change)
kuji watch game.fuji

# REPL
kuji

# Package manager
kuji pkg install <package>
kuji pkg search <query>
kuji pkg list

# Generate C wrapper
kujiwrap mylib.h -o mylib.fuji

# Format code
kuji fmt game.fuji

# Run tests
kuji test tests/*.fuji

Learning Curve
Day 1: Hello World
javascriptprint("Hello, World!");
Day 2: Variables and Functions
javascriptlet score = 0;

func add_points(points) {
    score += points;
    print("Score:", score);
}

add_points(10);
Day 3: Objects and Arrays
javascriptlet player = {
    name: "Jesse",
    health: 100,
    inventory: ["sword", "shield"]
};

player.health -= 10;
player.inventory.push("potion");
Week 1: Using Libraries
javascript#include <raylib>

func main() {
    raylib.InitWindow(800, 600, "My First Game");
    // ... game code
}
Week 2: Creating Libraries
javascript// player.fuji
func create(name) {
    return { name: name, health: 100 };
}

func damage(player, amount) {
    player.health -= amount;
}

// game.fuji
#include "player.fuji"

let p = player.create("Hero");
player.damage(p, 10);
Month 1: Wrapping C Libraries
bashkujiwrap mylib.h -o mylib.fuji
javascript#include "mylib.fuji"
// Now use any C library in Fuji!

Why Fuji Will Win
- Easiest to learn - JavaScript syntax everyone knows
- Easiest to extend - kujiwrap any C/C++ library in seconds
- Fastest to prototype - Bytecode VM for instant feedback
- Fastest in production - Compiles to native C
- Best interop - Seamless C/C++ integration
- Zero friction - No build systems, no config files
- Batteries included - Rich standard library
- Massive ecosystem - Instant access to all C/C++ libraries
Fuji makes it trivial to wrap existing C libraries, making decades of proven code immediately accessible.