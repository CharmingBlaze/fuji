// Intentionally invalid — `fuji check` / sema should report several issues (used by CI).
let dup = 1;
let dup = 2;

func needsThree(a, b, c) {
    return a;
}

needsThree(1);

func body() {
    aa;
    bb;
}
