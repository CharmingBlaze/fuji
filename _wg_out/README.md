# Minimal Fuji bindings

Auto-generated by **fujiwrap** 0.2.0-dev from `minimal.h`. **2** functions wrapped.

## Quick start

```fuji
#include "@minimal"

func main() {
    // Call library functions by name after linking wrapper.c
}
```

## Building

Set **`FUJI_NATIVE_SOURCES`** to **`wrapper.c`** and **`FUJI_LINKFLAGS`** so Clang finds the vendor headers and library, then:

```powershell
$env:FUJI_NATIVE_SOURCES = ".\wrapper.c"
$env:FUJI_LINKFLAGS = "-I<include> -L<lib> -lminimal"
fuji build game.fuji -o game.exe
```

```bash
export FUJI_NATIVE_SOURCES="$PWD/wrapper.c"
export FUJI_LINKFLAGS="-I<include> -L<lib> -lminimal"
fuji build game.fuji -o game
```

## Function categories

| Category | Count | Description |
|----------|-------|-------------|
| Miscellaneous | 2 | Auto-grouped from name prefixes. |

## Full API reference

See [api_reference.md](api_reference.md).

## License

These bindings are generated. The underlying **minimal** library retains its own license — see the upstream header.
